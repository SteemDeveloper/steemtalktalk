import React from 'react';
import styled from 'styled-components';

const LogoText = styled.Text`
  font-family: 'Sweet_Sensations_Personal_Use';
  font-size: 60;
  margin-bottom: 50;
  color: #242424;
`;

export default LogoText;
